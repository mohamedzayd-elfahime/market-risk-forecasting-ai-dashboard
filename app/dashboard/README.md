# Dashboard Frontend

The dashboard is a static frontend served by FastAPI.

Structure:

- `index.html`: page markup and dashboard panels
- `assets/css/app.css`: visual design and responsive layout
- `assets/js/app.js`: API calls, chart rendering, Admin actions and chatbot UI

There is intentionally no build step. Keep this frontend dependency-light and browser-native.
